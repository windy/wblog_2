# Command to run when "Run" button clicked
run_command: bin/dev

# Command to install or update dependencies
dependency_command: gem install bundler && bundle install && npm install yarn -g && yarn install